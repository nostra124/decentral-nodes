# Vectors — TAP-format published-test-vector suites

These tests exist to verify implementations against the canonical
test vectors published with each spec. They use TAP (Test Anything
Protocol) format and are run with `prove`.

Today: bitcoin BIP vectors (base58, secp256k1, BIP-0032..0350).
Future: any other package whose primitives have published vectors.
