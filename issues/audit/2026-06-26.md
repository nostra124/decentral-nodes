---
type: audit
date: 2026-06-26
auditor: claude (session 019ktvN3fm47VQzysSED7AdE)
status: closed
---

# Audit 2026-06-26 — self-hosting nodes & infra traceability

Triggered while reviewing the open roadmap: a batch of features had been
built and merged on request without tracking issues, violating CLAUDE.md
§13 (every observable behaviour must trace to a closed issue in `done/`).

## Gap found

| Shipped behaviour | Where | Tracking issue at audit time |
|---|---|---|
| Permanent podman in web sessions (`.claude/hooks/session-start.sh`) | PR #124 | **none** |
| `forgejo-node` (forge + Actions runners, per-platform labels) | PR #124 | **none** |
| `contrib/Install-ForgejoRunner.ps1` (Windows runner installer) | PR #125 | **none** |
| `webmin-node` (web sysadmin UI, :10000) | PR #128 | **none** |
| `usermin-node` (per-user web UI, :20000) | PR #128 | **none** |

Separately confirmed already-traced this session: the `-node` rename
test breakage (BUG-056) and the kcov coverage job (BUG-057) were filed
and closed; 13 stale "open" feature files that were in fact implemented
were retired to `done/` during the milestone triage (PR #126).

## Backfill filed

- FEAT-309 — permanent podman in web sessions → `feature/done/`
- FEAT-310 — forgejo-node (incl. the Windows runner installer) → `feature/done/`
- FEAT-311 — webmin-node → `feature/done/`
- FEAT-312 — usermin-node → `feature/done/`
- `ROADMAP-3.5.0.md` — groups FEAT-309..312 as the "self-hosting nodes
  & infrastructure" minor; the atomic-swap draft (FEAT-306) was shifted
  3.5.0 → 3.6.0 to free the slot for these shipped features.

## Residual

- **Version drift.** `VERSION` is `3.3.2` while 3.4.0/3.5.0-class work is
  already on master. Cutting the releases (VERSION bumps per
  `skills/version.md`) is an open packaging task, not a behaviour gap.
- BUG-043 (SIT CGI/LNURL/walkthrough suites) remains the open item for
  3.3.3; the 3.4.0 test/CI-hardening cluster (FEAT-050/051/052/053,
  FEAT-304) is unstarted.
