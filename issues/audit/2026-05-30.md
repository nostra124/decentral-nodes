---
type: audit
date: 2026-05-30
auditor: claude (session 01HQc5DvXYCyaGHFMxWAaJuB)
status: closed
---

# Audit 2026-05-30 — agent skill surface

Targeted audit of the agent-facing skill surface (FEAT-019), prompted
by an upstream change to the rpk skill convention. Scope: does the
shipped `bitcoin-wallet` skill still trace to reality, and do the
install scripts still match the rpk packaging contract?

## Surface walked

### `skills/bitcoin-wallet/{SKILL.md,opencode.md}`

| Claim in skill | Reality at 1.33.0 | Gap |
|---|---|---|
| PSBT via `psbt finalize/extract/decode` | `psbt` deprecated 1.23.0, removed 1.24.0 (FEAT-035); now `tx {finalize,extract,decode}` → `bip174` | **Stale** |
| Send pipeline `wallet build/sign/broadcast` | canonical noun is `tx` (FEAT-036) | **Stale** |
| `descriptor checksum/verify` | moved to `bip380` (FEAT-035) | **Stale** |
| `--mainnet` guard "isn't shipped" | shipped in FEAT-014 (config `network=mainnet` gate) | **Stale / unsafe** |
| No `utxo`/`tx bump`/`address`/`price`/`tax`/`watch` recipes | all shipped (FEAT-037/043/046/047/040/038-039/045) | **Missing coverage** |
| Backends: only mempool full | still true; `daemon install` (FEAT-033) ships but the `bitcoind` *backend* RPC is still a stub | Accurate |
| Paths `~/.local/var/bitcoin/wallets/<name>`, seed `secret <name>/seed`, path `m/84h/0h/0h/0/<i>` | accurate | OK |

### Install scripts vs. the rpk skill convention (`nostra124/rpk`, `docs/PACKAGING.md`)

| Item | Reality | Gap |
|---|---|---|
| rpk now installs skills to **claude + raven + opencode** (Claude & Raven share `SKILL.md`) | bitcoin installed only claude + opencode | **Missing Raven target** (FEAT-019 deferred it) |
| `install-skills-user` opencode symlink | pointed at `share/opencode/skills/<name>` (wrong subdir, no `.md`) → dangling | **Bug** |
| `CLAUDE.md` §2 BIP vendoring path | said `share/doc/bitcoin/standards/`; actual dir is `bips/` (what `cite()` reads) | **Doc bug** |

## Disposition

All gaps closed under **FEAT-048** (skill refresh + Raven target +
opencode symlink fix + CLAUDE.md corrections), landed with regression
tests in `tests/unit/bitcoin.bats`. No new forward gaps left open.

---

# Audit 2026-05-30 (part 2) — test surface

Second pass the same day, scope: the test suite and its CI wiring.
444 unit `@test`s across 10 `.bats` files, plus SIT (podman) and `.t`
vector tests. Prompted by this session's BUG-020/021/022 — stale or
buggy tests that reddened (or silently passed) the gate.

## Findings

| # | Finding | Severity | Disposition |
|---|---|---|---|
| 1 | `release-tag.yml` cut the tag on any push to master, not gated on `tests` — releases shipped on red (v1.34.0) | high | BUG-023 (fixed) |
| 2 | 9 assertions (all in bitcoin.bats) neutralised by trailing `\|\| true` — error-message contracts never enforced | medium | BUG-024 (fixed) |
| 3 | No lint in CI though `make lint` / `lint-cmd-names` exist (BUG-013/014/016 class) | medium | FEAT-049 — `lint` job added (cmd-names gating, shellcheck advisory) |
| 4 | Hand-crafted hex fixtures (45+ literals) with no consistency check — BUG-021's 83-vs-82-byte `UNSIGNED_TX` slipped through | medium | FEAT-050 (open) |
| 5 | External-tool env skew masks failures (missing `xxd`/`dc` changed the failure shape during repro) | low | FEAT-051 (open) |
| 6 | `shellcheck -S warning` not clean (bip350 array findings) so can't gate yet | low | FEAT-052 (open) |
| 7 | `bitcoin.bats` (223) + `streamline.bats` (138) monolithic | low | FEAT-053 (open) |
| 8 | master tip `ad723c4` moved the skill source to `.rpk/skills/` + deleted `opencode.md`, leaving 7 FEAT-019/048 tests red on the old path (red suite reached master again) | high | BUG-025 (fixed) |
| 9 | same migration left the Makefile skill-install globbing the deleted `skills/<name>/{SKILL.md,opencode.md}` (CI-skipped, so invisible) | medium | FEAT-054 (open) |
| - | Vector `.t` baseline never runs in CI | note | blocked by FEAT-025 (skip-don't-vendor); not re-litigated |

## Kept (working well)

`curl_fixture` / `build_*` fixture helpers; graceful `skip` for absent
tooling (`skip mandoc`, check-vectors); the PR failure-log comment; the
red-then-green bug discipline.

## Disposition

Fixed this pass: BUG-023, BUG-024, BUG-025, FEAT-049. Open follow-ups
filed: FEAT-050..054. The headline outcome — releases can no longer
outrun a green suite (BUG-023) — closes the systemic gap behind
BUG-020/021/025 (three red-suite-on-master incidents in one window).
