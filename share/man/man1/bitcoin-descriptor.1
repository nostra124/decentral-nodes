.\" bitcoin-descriptor(1) — partially deprecated.
.\"
.\" The checksum / verify / derive subcommands moved to
.\" bitcoin-bip380(1) in 1.23.0 (FEAT-035 Stream B); the standalone
.\" 'descriptor' verb continues to work as a deprecated alias for
.\" those three (removal target: 1.24.0).
.\"
.\" 'bitcoin descriptor wallet <name>' is NOT deprecated — it stays
.\" in bitcoin(1) as a wallet-touching helper. See bitcoin-wallet(1).
.\"
.\" This man page resolves to bitcoin-bip380(1) via .so per the
.\" FEAT-041 alias convention for the deprecated paths.
.so man1/bitcoin-bip380.1
